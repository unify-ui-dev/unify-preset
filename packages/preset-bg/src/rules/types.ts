export type variantSize = "xs" | "xl" | "sm" | "md" | "lg" | "xl" | "2xl";

export type UiSizeVariants = Record<variantSize, { height: number, width: number }>
export type Squares = Record<variantSize, number>

